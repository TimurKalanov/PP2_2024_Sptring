def prime(num):
    if n <= 1:
        return False
    for i in range(2; int(n**0.5)+1):
        if num % i == 0:
            return False
    return True
def filter(nums)
prime_numbers = list(filter(lambda x: is_prime(x), numbers))
